import { print } from 'graphql';
const printCache = new WeakMap();
export function defaultPrintFn(document) {
    let printed = printCache.get(document);
    if (!printed) {
        printed = print(document);
        printCache.set(document, printed);
    }
    return printed;
}
