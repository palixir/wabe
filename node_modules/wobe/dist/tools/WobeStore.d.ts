/// <reference types="bun-types" />
export interface WobeStoreOptions {
    interval: number;
}
/**
 * WobeStore is a class that stores data for a certain amount of time
 */
export declare class WobeStore<T> {
    private options;
    private store;
    intervalId: Timer | undefined;
    constructor(options: WobeStoreOptions);
    _init(): void;
    set(key: string, value: T): void;
    get(key: string): T | undefined;
    clear(): void;
    stop(): void;
}
