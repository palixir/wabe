import type { WobeHandler } from '../Wobe';
interface BodyLimitOptions {
    maxSize: number;
}
/**
 * bodyLimit is a hook that checks if the request body is too large
 */
export declare const bodyLimit: (options: BodyLimitOptions) => WobeHandler;
export {};
