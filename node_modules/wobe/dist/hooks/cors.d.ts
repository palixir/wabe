import type { WobeHandler } from '../Wobe';
type Origin = string | string[] | ((origin: string) => string | undefined | null);
export interface CorsOptions {
    origin: Origin;
    allowMethods?: string[];
    allowHeaders?: string[];
    maxAge?: number;
    credentials?: boolean;
    exposeHeaders?: string[];
}
/**
 * cors is a hook that adds the necessary headers to enable CORS
 */
export declare const cors: (options?: CorsOptions) => WobeHandler;
export {};
