{
    "name": "wibe-mongodb-launcher",
    "version": "0.0.1",
    "license": "MIT",
    "main": "src/index.ts",
    "description": "Package to launch the mongodb for test",
    "scripts": {
        "dev": "bun index.ts --watch",
        "start": "bun index.ts",
        "build": "bun build index.ts --minify --target=bun --outdir lib",
        "linter": "biome lint ./src/**/**/*.ts --config-path=../../ --no-errors-on-unmatched",
        "formatter": "biome format --config-path=../../ --write ."
    },
    "dependencies": {
        "tcp-port-used": "1.0.2",
        "mongodb-memory-server": "8.15.1"
    },
    "devDependencies": {
        "@types/bun": "1.0.0",
        "@types/tcp-port-used": "1.0.4"
    }
}
