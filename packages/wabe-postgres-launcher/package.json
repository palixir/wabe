{
  "name": "wabe-postgres-launcher",
  "version": "0.0.1",
  "description": "Package to launch PostgreSQL for testing",
  "main": "index.ts",
  "scripts": {
    "dev": "bun index.ts --watch",
    "start": "bun dist/index.js",
    "test:pg-mem": "bun test testInMemoryPostgres.ts --watch",
    "ci": "bun test index.test.ts --verbose",
    "build": "bun build index.ts testInMemoryPostgres.ts --target=bun --outdir dist",
    "lint": "biome lint ./*.ts --no-errors-on-unmatched",
    "format": "biome format --write .",
    "test": "bun test index.test.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.13.0",
    "pg-mem": "^3.0.3",
    "tcp-port-used": "^1.0.2"
  },
  "devDependencies": {
    "@types/bun": "1.0.0",
    "@types/tcp-port-used": "1.0.4",
    "@types/pg": "^8.11.10"
  }
}
