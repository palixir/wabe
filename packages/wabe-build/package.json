{
    "name": "wabe-build",
    "version": "0.5.0",
    "license": "MIT",
    "main": "src/index.ts",
    "description": "Utils package to build other packages",
    "scripts": {
      "build": "if [ -z \"$CI_BUILD\" ]; then bun run src/index.ts; else echo \"CI_BUILD is defined, skipping build\"; fi"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "oxc-transform": "0.58.1"
    }
}
