pre-commit:
    parallel: true
    commands:
        check:
            glob: '*.{js,ts,jsx,tsx,graphql}'
            run: bun oxlint {staged_files} && bun oxfmt --write {staged_files} && git add {staged_files}
